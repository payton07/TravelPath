<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TravelPath" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main colors -->
        <item name="colorPrimary">@color/emerald_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/emerald_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/bg_main</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:windowBackground">@color/bg_main</item>

        <!-- Status bar and navigation bar colors -->
        <item name="android:statusBarColor">@color/bg_main</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/bg_main</item>
    </style>

    <style name="Theme.TravelPath" parent="Base.Theme.TravelPath" />
</resources>
